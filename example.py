import requests  # Error 1: Missing library (dependency error)

def calculate_ratio(a, b):
    return a / b  # Error 2: Missing colon (syntax error)

def process_data(data):
    # Error 3: Division by zero (runtime error)
    for item in data
        print(item)
    return calculate_ratio(100, 0)

def main():
    print("Starting process...")
    process_data([1, 2, 3])

if __name__ == "__main__":
    main()


# Context: [the terminal background]
# Error: [the red texts]
# Requests: Fix it.