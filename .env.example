# Runtime (Koyeb / local). Never commit real secrets — AI_BUILDER_TOKEN is injected on ai-builders.space deploy.
AI_BUILDER_TOKEN=
AI_BUILDER_BASE_URL=https://space.ai-builders.com/backend

# Optional local default (uvicorn / Docker use CMD with PORT)
# PORT=8000
